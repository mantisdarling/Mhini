Code-splitting findings, 2026-08-22

The original 683 KB main client bundle is now a 465 KB entry chunk, with the public Home route deferred into a 64 KB Home chunk and the private Studio route deferred into a 123 KB ProjectConsole chunk. Privacy and NotFound are also separate lazy route chunks. Shared motion, query, UI, and profile data remain predictable vendor chunks.

The public Home route renders correctly at a 390px phone viewport. The deferred Studio route resolves beyond the loading shell to the intended unauthenticated sign-in state, preserving the authentication boundary. The route loading shell is lightweight, accessible through status semantics, and uses the existing dark and red visual system. 37 tests pass, TypeScript passes, production build passes, and the high-severity dependency audit reports no known vulnerabilities.
