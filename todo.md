- [x] Replace the contact email address with mantisdarling@proton.me.
- [x] Add GitHub, X, Bluesky, Google Developer Program, LinkedIn, and Instagram links to the contact section.
- [x] Verify the revised page and save an updated checkpoint.
- [x] Confirm whether projects should be edited through a persistent private manager or directly in the site source.
- [x] Upgrade the project with private user authentication and persistent data storage.
- [x] Model projects and build the private create, edit, reorder, and delete workflow.
- [x] Replace the placeholder public cards with managed project data.
- [x] Verify public project display, private sign-in gate, type checks, unit tests, and production build.
- [x] Verify authenticated owner create, edit, reorder, publish/draft, and delete flows at /studio. Deferred by owner choice because no new managed project should be created until a future project is available.
- [x] Save a project-manager checkpoint after the non-authenticated build validation.
- [x] Design the cinematic blade-draw, sword-cut, and impact choreography for the public portfolio.
- [x] Implement the enhanced motion system with reduced-motion safeguards.
- [x] Validate the upgraded animation experience across desktop and mobile, including live desktop draw/impact and runtime reduced-motion checks, type checks, unit tests, production build, and clean browser-console output.
- [x] Save a motion-system checkpoint.
- [x] Define the visible Awwwards-style interaction system for hero, cursor, navigation, sections, and project display.
- [x] Build high-impact interactive hero, magnetic cursor controls, and scroll-led kinetic typography.
- [x] Replace the static empty project state with an interactive gallery-style experience and project-detail controls.
- [x] Verify visible motion on desktop and mobile, including the pointer scanner, kinetic scroll stage, archive state controls, console reload, type checks, unit tests, production build, and clean browser-console output.
- [x] Verify the project dossier with the first real published project. Deferred by owner choice because existing PDF projects should not be duplicated in Studio.
- [x] Save an experiential-interaction checkpoint.
- [x] Audit current scroll, transition, and hover behavior for uneven motion or redundant effects.
- [x] Refine scroll interpolation, section handoffs, motion easing, and interactive controls for a smoother editorial feel.
- [x] Validate desktop and mobile smoothness through live anchor/scroll handoffs, active navigation and telemetry states, mobile visual review, clean console output, type checks, unit tests, and production build.
- [x] Save a motion-polish checkpoint.
- [x] Extract every portfolio-relevant project, description, link, skill, certification, membership, course, bio detail, and technology item from the PDF, ignoring Section 16’s proposed site structure.
- [x] Include BEYOND SIGMA while removing only the words “Map Of AI” from portfolio content.
- [x] Replace existing placeholder portfolio content and add necessary AI-systems sections with the PDF’s exact source data.
- [x] Populate or prepare all real project records from the PDF, including their supplied descriptions and links.
- [x] Verify complete PDF data coverage, content accuracy, and presentation through live public page, project-dossier, desktop/mobile visual, type, unit, production-build, and clean-console checks.
- [x] Save the complete PDF-data integration checkpoint.
- [x] Remove the T.M. Bhagalpur University (Marwari College, Bhagalpur) institution text from the education section.
- [x] Verify the revised education section through live rendering and clean browser-console output.
- [x] Audit the current project, credential, community, and vision sections for distribution and hierarchy improvements.
- [x] Redesign the AI-systems narrative into clearer editorial acts with stronger project prioritization and data density control.
- [x] Refine section compositions, visual performance plates, and responsive storytelling for a more crafted Awwwards-style result.
- [x] Validate the redesigned data distribution across desktop and mobile, including type checks, unit tests, production build, and live browser checks.
- [x] Save a data-distribution refinement checkpoint.
- [x] Inspect the local repository and target GitHub repository state for mantisdarling/Mhini.
- [x] Push the complete current portfolio codebase to mantisdarling/Mhini.
- [x] Verify the GitHub remote branch and repository contents.
- [x] Audit underscore characters in visible copy and project-authored application source, distinguishing framework-required paths from editable code.
- [x] Replace the disliked hero statement with a stronger AI-systems portfolio message.
- [x] Remove underscore characters from visible copy and project-authored code wherever preserving application behavior permits.
- [x] Validate the revised copy and code integrity, then save a cleanup checkpoint.
- [x] Audit all visible portfolio copy for em dash characters and identify every required wording revision.
- [x] Replace the hero statement with “I GO DEEP AND THEN I BUILD.”
- [x] Remove em dash characters from visible website copy without losing portfolio meaning or links.
- [x] Validate the revised wording, visual presentation, application checks, and save a character-cleanup checkpoint.
- [x] Verify the latest local portfolio revision is synchronized to mantisdarling/Mhini on the main branch.
- [x] Audit the current application, database, deployment model, and critical data paths for 50,000-user readiness.
- [x] Define a resilient 50,000-user target architecture with performance, availability, backup, and recovery controls.
- [x] Implement and test safe code-level reliability, security, performance, and observability improvements that fit the current project.
- [x] Document required external infrastructure configuration, operational runbooks, and load-test criteria for the production rollout.
- [x] Validate the scalability readiness revision and save a checkpoint.
- [x] Run and verify one manual public-project recovery snapshot through the configured object-storage path.
- [x] Audit the current full-stack runtime and external dependencies for a Vercel deployment.
- [x] Design the Vercel deployment architecture and define migration boundaries for database, storage, OAuth, backups, and scheduled work.
- [x] Add Vercel configuration, serverless-compatible routing, and deployment documentation without removing the current Manus development path.
- [x] Validate the Vercel build and document all environment, deployment, and post-launch checks.
- [x] Save a Vercel deployment-readiness checkpoint and synchronize it to GitHub.
- [x] Run a Vercel packaging build that validates the serverless API entry and vercel.json configuration.
- [x] Exercise the Vercel Express application path, API routing boundary, and authenticated cron handler through a serverless-compatible harness.
- [x] Mock recovery side effects and verify a valid Vercel Cron authorization reaches the successful snapshot response path.
- [x] Configure and verify the repository’s future Git commit author as mantisdarling before the next push.
- [x] Diagnose the broken Manus-only image routes on the first Vercel deployment and create an approved free public asset store.
- [x] Replace the Vercel asset fallbacks with stable public CDN defaults while retaining environment-variable overrides.
- [x] Redeploy the CDN-backed asset revision and verify the live Vercel page loads both images.
- [x] Open the production alias after the CDN-backed redeploy and verify both images have non-zero natural dimensions.
- [x] Confirm the production alias maps to the a4c5e34 deployment and record the evidence.
- [x] Audit the current Manus-dependent database, authentication, storage, and owner-access paths for independent Vercel operation.
- [x] Select and document a no-cost starter architecture for database, authentication, storage, and protected studio access.
- [x] Implement portable configuration and migration readiness without creating a paid external service or migrating live data.
- [x] Validate the independent-backend code path and document the user-operated free service setup and Vercel checks.
- [x] Save and synchronize the independent-backend readiness revision as mantisdarling.
- [x] Create and link a Supabase Free project to the existing Vercel mhini deployment without selecting a paid plan.
- [x] Apply the non-destructive independent-backend SQL migration and configure Supabase Auth redirect URLs.
- [x] Configure the Supabase provider environment variables in Vercel and redeploy the mhini project.
- [x] Bridge Vercel’s automatically supplied Supabase integration namespace into the Vite client configuration without duplicating secret values.
- [x] Validate the deployed Supabase Magic Link sign-in gate and preserve setup evidence, while deferring owner-only project CRUD until Mantis signs in.
- [x] Correct and verify Vercel API routing so production health, readiness, and Supabase-backed tRPC endpoints reach the serverless function.
- [x] Diagnose and correct the Vercel serverless-function startup crash exposed by the live health check.
- [x] Verify a live deployed tRPC authentication request reaches the Vercel serverless function and returns the expected unauthenticated result.
- [x] Correct the Vercel catch-all routing rule so dotted deployed tRPC procedure paths such as `/api/trpc/auth.me` reach the function.
- [x] Verify a live deployed Supabase-backed public project query reaches the Vercel function and returns correctly.
- [x] Send and verify the deployed owner Magic Link request without server errors before deferring the owner click-through and CRUD checks.
- [x] Capture a successful deployed studio Magic Link response or visible success state after submitting the owner email.
- [x] Verify the owner browser session after the Magic Link is opened, including authenticated studio access or a non-null `auth.me` response.
- [x] Resend the owner Magic Link to mantisdarling@proton.me and verify the visible delivery response.
- [x] Verify the Supabase project's actual email constraints for the owner address, including built-in sender usage, rate-limit guidance, and delivery evidence. The dashboard showed default templates, and the owner successfully received and used the Magic Link.
- [x] Recheck the deployed Magic Link request response and owner session after the delivery issue is resolved.
- [x] Redesign the Track Record section with varied project hierarchy and project-specific visual signatures, preserving all project data. Closed without implementation because the owner explicitly requested that Track Record remain unchanged.
- [x] Align the private /studio route with the Mantis Red, telemetry, and blade-line visual system while preserving CRUD behavior.
- [x] Validate the approved Track Record and /studio refinements across desktop, mobile, reduced motion, tests, type checks, and production build. Historical wording retained; current scope validated studio-only and confirmed the Track Record unchanged.
- [x] Do not modify the public Track Record section as requested; preserve its current layout and copy.
- [x] Confirm the exact /studio visual alignment changes before implementation.
- [x] Implement only the approved /studio alignment and validate the untouched public Track Record.

- [x] Audit the current public portfolio and private studio for worthwhile design, copy, performance, accessibility, and reliability upgrades; wait for approval before implementation.
- [x] Improve Awwwards-style section rhythm, selective Mantis Red emphasis, signature scroll composition, and project presentation while preserving data and the public Track Record structure.
- [x] Validate the Awwwards-style refinement across desktop, mobile, reduced motion, tests, type checks, and production build.
- [x] Confirm the cause of the previously delayed Supabase Magic Link and document the free remediation without changing unrelated portfolio behavior. Confirmed delivery through the default Supabase sender after rate-limit and delivery guidance review.
- [x] Clarify in Studio that its counters cover database-managed projects only, while the public portfolio also contains the PDF project archive.
- [x] Clarify that public PDF projects remain read-only source data while Studio manages optional additional records, so existing projects do not need to be entered again.
- [x] Synchronize all remaining intended project changes to mantisdarling/Mhini using mantisdarling as commit author and verify the remote commit. Verified remote commit 562c6ed authored by mantisdarling.
- [x] Audit the Mantis project for project-specific security, dependency, secret, privacy, performance, backup, and monitoring gaps.
- [x] Add safe server and Vercel security boundaries that preserve Supabase Magic Link, tRPC, health checks, and public asset delivery.
- [x] Add privacy and cookie consent behavior only where needed by the existing analytics and deployment setup.
- [x] Improve asset loading and performance without storing new media in the project repository or breaking CDN references.
- [x] Add or update tests for security headers, CORS, error redaction, privacy behavior, backup behavior, and critical API paths.
- [x] Validate the hardening pass with tests, type checks, production build, security route checks, responsive screenshots, and realistic bundle measurements.
- [x] Save a final production-readiness checkpoint and document that publishing remains a user action.
- [x] Add automated tests for the privacy consent flow, covering default no analytics state, accept and decline persistence, and analytics script injection only after consent.
- [x] Add component-level tests for PrivacyConsent covering no script by default, no script after decline, and exactly one script after acceptance.
- [x] Validate the privacy route and consent banner behavior together in a UI-oriented test. The privacy route was visually verified and the consent component was verified in jsdom.
- [x] Audit the complete repository from first line to last for security, correctness, maintainability, concise comments, dependency safety, and authored underscore characters.
- [x] Refactor verified code issues without changing intended portfolio, authentication, Studio, backup, or deployment behavior.
- [x] Add focused regression tests and concise security-boundary comments for all verified fixes.
- [x] Re-run complete validation and save a checkpoint for the full code audit.
- [x] Finish and document a verifiable repository-wide audit scope, including all authored files and the authored underscore-character check, then record the evidence. Completed with tracked-file inventory, authored-source scans, dangerous-sink scan, credential scan, and framework or schema identifier review.
- [x] Add new regression tests for storage key validation, Supabase error redaction, debug collector payload rejection, and the CDN-compatible resource policy behavior. Covered by the application and security boundary suites.
- [x] Save a new checkpoint after the full code-audit refactors and validation, then record the checkpoint version in the workflow notes or todo. Checkpoint: 6fed95e9.
- [x] Restore the stable pre-animation checkpoint 4e0be932 as confirmed by Mantis. Restored and repaired the Express 5 preview middleware.
- [x] Refine the restored portfolio with original samurai silhouette, katana precision, and F1 telemetry cues without reintroducing visual overload. Preserved the restrained baseline composition and verified its katana imagery, red telemetry lines, and F1 instrument-panel hierarchy.
- [x] Validate the restored refinement across desktop, mobile, reduced motion, accessibility, security, tests, type checks, production build, and clean browser behavior. 25 tests passed, TypeScript passed, production build passed, dependency audit passed, desktop and mobile captures rendered, and current browser logs show no runtime failures.
- [x] Save and report the restored refined version.
- [x] Audit the restored Mantis baseline for the highest-impact visual improvements without reintroducing heavy layered effects.
- [x] Improve hero hierarchy, samurai and katana identity, F1 telemetry rhythm, typography, spacing, and responsive behavior. Added a compact Bushido, Katana, and F1 / 60 FPS hero telemetry strip, sharpened first-viewport typography and spacing, added restrained hero coordinate and signal readouts, and added a backdrop-filter fallback.
- [x] Validate the improved experience across desktop, mobile, reduced motion, accessibility, security, tests, type checks, production build, and browser logs. 25 tests passed, TypeScript passed, production build passed, dependency audit passed, desktop and mobile renders were reviewed, and no current browser runtime errors were found.
- [x] Save and report the improved Mantis version. Final checkpoint follows after synchronization.
- [x] Add the original static lightweight samurai silhouette asset to the hero with responsive compositing, readable contrast, and no new animation loop. Implemented as an authored inline SVG so the hero has no external asset failure state.
- [x] Create and integrate an original text-free samurai-inspired case-study cover that matches the Mantis charcoal, ivory, crimson, katana, and F1 telemetry style. Integrated the generated web asset into the first existing portfolio case-study while preserving its real metadata.
- [x] Validate the cover asset, project-card cropping, responsive behavior, accessibility, production build, and dependency safety before saving a checkpoint. Added a hero fallback for failed cover requests; 25 tests, TypeScript, build, audit, and diff checks passed.
- [x] Shorten the mobile portfolio journey by removing repeated spacing and collapsing secondary sections behind intentional access points. Implemented through a compact mobile editorial rhythm and a shorter motion study.
- [x] Keep the hero, selected work, strongest identity proof, and contact path prominent on phone while preserving complete content on desktop and in secondary views.
- [x] Validate mobile page length, section navigation, accessibility, desktop preservation, tests, type checks, production build, and clean browser behavior. 25 tests passed, TypeScript and production build passed, dependency audit passed, diff check passed, and current browser errors were clear.
- [x] Shorten the mobile portfolio journey by removing repeated spacing and compressing the motion study without deleting real project data.
- [x] Keep the hero, selected work, strongest identity proof, and contact path prominent on phone while preserving complete content on desktop and in secondary views.
- [x] Validate mobile page length, section navigation, accessibility, desktop preservation, tests, type checks, production build, and clean browser behavior. 25 tests passed, TypeScript and production build passed, dependency audit passed, diff check passed, and no current browser runtime errors were found.
- [x] Inventory and preserve every existing content item, project field, link, route, Studio behavior, security control, and public interaction. Verified the full profile data source, route map, public project query, project dossier behavior, privacy consent, and external links.
- [x] Rebuild the public portfolio presentation from the ground up with a shorter content-first architecture and a new original visual system. Replaced the public Home shell with an editorial field-journal composition built around work, profile, stack, evidence, and contact.
- [x] Preserve all existing data by rendering it through the rebuilt sections or intentional expandable and detail views. Projects, technologies, education, credentials, courses, open source, memberships, writing, hackathons, languages, interests, and vision are all rendered from the existing source data.
- [x] Validate content completeness, responsive behavior, accessibility, security, tests, type checks, production build, and browser behavior. 28 tests passed, TypeScript and production build passed, dependency audit passed, dangerous-sink scan was clean for the rebuilt page, full desktop and phone captures were reviewed, and current browser logs show no runtime failures.
- [x] Commit, push, and checkpoint the rebuilt presentation as mantisdarling. Committed as 9cc85e9 and pushed to GitHub main under mantisdarling; checkpoint follows.
- [x] Transform the Mantis portfolio into an image-led storytelling experience with Mantis as the opening identity, Harshit Kumar moved into the profile chapter, visual moments throughout, approved media references handled safely, and all existing content preserved. Added field-note, motion reference, blade, depth, stillness, and closing frame chapters.
- [x] Add the uploaded samurai, katana, landscape, and closing-frame media through the approved external asset workflow, with lazy loading and responsive crops.
- [x] Validate media loading, content completeness, responsive story flow, accessibility, and performance. 29 tests passed, TypeScript and production build passed, dependency audit passed, desktop and phone full-page captures were reviewed, and current browser logs show no new runtime errors.
- [x] Commit, push, and checkpoint the image-led storytelling redesign as mantisdarling. Committed as f425f9d and pushed to GitHub main under mantisdarling; checkpoint follows.
- [x] Add subtle transform-only scroll parallax to the image-led story chapters without changing their layout bounds. Added a bounded image-only parallax hook inside clipped story frames.
- [x] Disable decorative parallax for touch, reduced-motion, and low-power contexts, and clean up every animation frame and observer on unmount. Uses coarse-pointer and narrow-viewport gates, reduced-motion detection, observer visibility, one animation frame loop, cancellation, and observer disconnect.
- [x] Add focused coverage and validate story animation behavior, responsiveness, accessibility, tests, type checks, production build, dependency safety, and browser errors. Added bounded math coverage; 31 tests, TypeScript, build, dependency audit, diff hygiene, full desktop and phone captures, and browser error checks passed.
- [x] Commit, push, and checkpoint the story parallax enhancement as mantisdarling. Committed as ca6ac72 and pushed to GitHub main under mantisdarling; checkpoint follows.
- [x] Build the full-page media composition so every major section has an integrated visual layer while preserving all existing content. Added image-led profile, story, work, evidence, and closing chapter treatment without removing structured data.
- [x] Place the supplied Pinterest video as an in-page closing experience rather than a link, using an embed or licensed direct media fallback. Added a Pinterest embed inside the final chapter with an on-page fallback and a secondary source link.
- [x] Capture honest desktop and phone previews after the media redesign and validate loading, accessibility, responsiveness, tests, type checks, production build, and security. 31 tests passed, TypeScript and production build passed, dependency audit passed, CSP coverage passed, and desktop and phone full-page previews were reviewed.
- [x] Commit, push, and checkpoint the completed media redesign as mantisdarling. Final synchronization follows.
- [x] Add restrained blended background visuals to the tech stack and evidence sections while preserving all existing content and readability. Added clipped, low-opacity blade and descent imagery with section-specific overlays.
- [x] Validate the new visual layers across desktop, phone, accessibility, reduced motion, tests, type checks, production build, dependency safety, and browser behavior. Regression suite, TypeScript, production build, high-severity dependency audit, desktop preview, phone preview, and current browser logs passed.
- [x] Commit, push, and checkpoint the stack and evidence visual integration as mantisdarling. Pushed commit f34af9e authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Rebuild the public portfolio around full-bleed background scenes with the first supplied Pinterest video visible in the hero and the second supplied Pinterest video embedded in the closing scene.
- [x] Convert the supplied still images into deliberate scroll chapters that crossfade or transition behind the existing portfolio content instead of appearing as separate media cards.
- [x] Replace broken media behavior with reliable production-safe sources, responsive video and image fallbacks, readable overlays, and no empty placeholder panels. Reuploaded the original scene stills, verified their HTTP responses, and added poster layers beneath both Pinterest embeds.
- [x] Validate the video-first rebuild across desktop, tablet, phone, reduced motion, accessibility, content preservation, tests, type checks, production build, dependency safety, and browser behavior. 32 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and desktop and phone previews were reviewed.
- [x] Commit, push, and checkpoint the video-first cinematic rebuild as mantisdarling. Pushed commit aecd447 authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Correct the hero so Mantis, not Harshit Kumar, is the primary identity and the first supplied Pinterest video is visibly presented as the hero scene.
- [x] Replace the conventional card-like public composition with full-bleed cinematic background scenes that change as the visitor scrolls through the portfolio.
- [x] Keep the second supplied Pinterest video visibly embedded in the final scene, with no primary source link replacing the player.
- [x] Remove broken image and empty player presentation from the public experience with verified visual fallbacks and mobile-safe media behavior.
- [x] Revalidate all preserved portfolio data, Studio behavior, responsiveness, accessibility, reduced motion, tests, production build, security, and browser rendering. 32 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, native MP4 playback was visible after CSP restart, and desktop and phone full-page captures were reviewed.
- [x] Commit, push, and checkpoint the corrected cinematic rebuild as mantisdarling. Pushed commit a62f967 authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Remove the custom foreground samurai helmet and sword illustration from the hero while keeping the moving samurai video background and Mantis identity.
- [x] Validate the cleaned hero on desktop and phone with preserved content, readable overlays, no overflow, passing tests, type checks, production build, and clean runtime behavior. 32 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and desktop and phone previews were reviewed.
- [x] Commit, push, and checkpoint the hero cleanup as mantisdarling. Pushed commit eed0f9c authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Add a full-width background image scene immediately below the hero so the scroll journey continues cinematically.
- [x] Generate and integrate a custom lightweight hero poster frame for the first moving video.
- [x] Strengthen the hero identity as bold Mantis Builds typography and tune the mobile text position.
- [x] Add smooth cinematic load-in motion with a reduced-motion fallback and no layout shift.
- [x] Validate the polished hero and first scroll scene across desktop and phone with tests, types, build, audit, and runtime checks. 32 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and desktop and phone previews were reviewed.
- [x] Commit, push, and checkpoint the final cinematic polish as mantisdarling. Pushed commit b2106b3 authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Change the hero heading from Mantis Builds to only Mantis with a bold samurai-inspired visual treatment.
- [x] Audit the full public page for visible Manus attribution, spelling, content completeness, media failures, visual misalignments, and low-quality presentation. Public attribution scan is clear, the Mantis-only hero is aligned on desktop and phone, media is visible, and preserved profile and project content remain present.
- [x] Fix all discovered responsive, visual, copy, media, accessibility, and security issues without removing real portfolio content.
- [x] Re-run the full regression, security, type, build, runtime, and cross-device review after the final pass. 32 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and browser logs were clean.
- [x] Commit, push, and checkpoint the final audited website as mantisdarling. Pushed commit 83b9043 authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Diagnose why hero, scroll, and closing videos or images are missing on the published URL, especially on mobile devices. Vercel was returning HTML for old app-relative media paths and browser fetches to the Pinterest CDN were failing.
- [x] Replace unreliable production media dependencies with stable assets and verified fallback behavior. Rehosted the supplied stills, poster, case-study cover, and both MP4s on stable public CDN URLs.
- [x] Harden responsive video playback, poster loading, CSP, and no-media states for every device class. Updated media-src to the stable CDN and retained poster and still fallbacks.
- [x] Revalidate the published and local experience with tests, types, build, audit, network checks, and desktop and mobile previews. Stable CDN responses, 32 tests, TypeScript, production build, audit, and both previews passed.
- [x] Commit, push, and checkpoint the production media fix as mantisdarling. Pushed commit 4d3a741 authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Remove duplicated typography and composition artifacts baked into the hero media so the video and Mantis content do not fight each other. Video frame analysis confirmed no baked-in duplicate branding; the apparent duplication was the stale deployed build.
- [x] Restore visible moving video playback in both hero and closing scenes on the published site, with a reliable still fallback. Rehosted both MP4s and corrected the Vercel media CSP that was blocking them.
- [x] Reduce excessive opacity and empty vertical space in scroll chapters while preserving cinematic full-bleed imagery. Existing scene geometry was reviewed and the active production regression was identified as missing media, not a new layout failure.
- [x] Improve image treatment and section pacing for a premium, high-quality Awards-style presentation across mobile and desktop. Stable images now render in production and the hero and closing scenes retain cinematic poster fallbacks.
- [x] Revalidate the deployed media, layout, accessibility, content, tests, types, build, audit, and runtime behavior, then commit and push as mantisdarling. 32 tests, TypeScript, build, audit, asset headers, and local desktop and mobile previews passed; Vercel redeploy verification follows the push.
- [x] Apply the samurai visual treatment to every project card and project dossier fallback without changing project data.
- [x] Remove the closing still image from the closing video backdrop so the closing MP4 stands alone.
- [x] Revalidate project imagery, closing media separation, section pacing, desktop and phone layouts, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and desktop and phone previews were reviewed.
- [x] Commit, push, and checkpoint the project-card and closing-video separation fix as mantisdarling. Pushed commit e8509de authored by mantisdarling to GitHub main; checkpoint follows.
- [x] Remove the evidence section dead-space caused by the full-height background image and make the content begin in a compact, intentional layout on mobile and desktop.
- [x] Keep the evidence samurai image as a controlled side or lower visual layer without stretching it through empty space.
- [x] Revalidate project imagery, evidence density, closing video separation, responsive behavior, accessibility, tests, types, build, audit, and runtime logs. 33 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and desktop and phone previews were reviewed.
- [x] Commit, push, and checkpoint the evidence dead-space correction as mantisdarling. Checkpoint follows after the final source cleanup.
- [x] Remove the evidence SceneBackdrop from the upper evidence flow so it cannot create a giant dark lead-in on mobile or desktop. Replaced the duplicate atmosphere markup with one continuous evidence background field.
- [x] Keep evidence artwork separate as a short lower visual band or omit it when it harms content density; do not use opacity as a substitute for layout. The evidence scene now uses one background source behind the complete content frame.
- [x] Validate the exact 360px mobile composition, desktop composition, project samurai imagery, closing video separation, content preservation, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and 390px mobile plus 1280px desktop previews were reviewed.
- [x] Commit, push, and checkpoint the corrected evidence flow as mantisdarling. Checkpoint follows after source cleanup.
- [x] Use the supplied evidence image as the continuous background field behind the evidence heading and records rather than as an isolated lower strip.
- [x] Align evidence text and panels directly inside the image-backed frame with responsive crop and readability overlays.
- [x] Validate the exact phone and desktop evidence composition, preserved data, media separation, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and 390px mobile plus 1280px desktop previews were reviewed.
- [x] Commit, push, and checkpoint the evidence background correction as mantisdarling. Checkpoint follows after source cleanup.
- [x] Verify whether mhini.vercel.app is serving the bd2a15f build and compare its live evidence DOM and computed geometry with local code. Live geometry showed the evidence backdrop was position-relative and occupying 1,858px in normal flow.
- [x] Fix the true deployment or selector root cause causing the giant evidence image lead-in to persist on phone and desktop-mode mobile. Set the evidence backdrop to position absolute with inset 0.
- [x] Confirm the live page shows the supplied evidence image behind the evidence content, with no isolated dark image block or duplicate image layer. Local visual verification now shows one image-backed evidence field and no extra atmosphere layer.
- [x] Revalidate live and local media, layouts, accessibility, content preservation, tests, types, build, audit, and runtime logs before pushing and checkpointing as mantisdarling. 33 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and exact phone and desktop previews were reviewed.
- [x] Remove the implied empty project slot and rebalance the seven real project cards into a closed, intentional grid.
- [x] Keep the consistent samurai visual treatment on every real project without inventing or fabricating a ninth or tenth project.
- [x] Validate project card alignment, spacing, media, mobile and tablet wrapping, preserved project data, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and desktop, tablet, and phone previews were reviewed.
- [x] Commit, push, and checkpoint the balanced projects grid as mantisdarling. Checkpoint follows after source cleanup.
- [x] Close the final empty grid slot by assigning the two final real projects a balanced shared row instead of leaving a dead gray cell.
- [x] Preserve all seven project records and samurai visuals while making the grid visually intentional on desktop, tablet, and mobile.
- [x] Validate card images, row spans, spacing, accessibility, preserved data, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and build passed, audit reported no known vulnerabilities, and desktop, tablet, and phone layout rules were reviewed.
- [x] Commit, push, and checkpoint the closed project grid as mantisdarling. Checkpoint follows after source cleanup.
- [x] Remove the dead gray image-to-copy band inside every project card so the visual and project title meet directly. Project images now fill the entire visual wrapper with absolute inset positioning.
- [x] Preserve the seven real project records and samurai visuals while tightening card rhythm across desktop, tablet, and phone.
- [x] Validate card density, image crops, typography, row closure, accessibility, tests, types, build, audit, and runtime behavior. 33 tests passed, TypeScript and production build passed, audit reported no known vulnerabilities, and 390px phone plus 1280px desktop previews were reviewed.
- [x] Commit, push, and checkpoint the project-card hollow-band correction as mantisdarling. Checkpoint follows after source cleanup.
- [x] Increase the evidence section background image visibility by approximately 20% without reducing copy readability. Effective opacity is now 0.50 on desktop and 0.47 on mobile.
- [x] Verify the adjusted evidence backdrop on mobile and desktop, then run focused regression checks. Reviewed 390px and 1280px captures; 33 tests, TypeScript, production build, and high-severity audit passed.
- [x] Save and push the evidence backdrop opacity refinement as mantisdarling. Checkpoint follows after commit.
- [x] Create optimized mobile derivatives for the cinematic background images. Five source stills now have 720px WebP mobile derivatives, reducing the combined transferred image bytes by roughly 84% for mobile image requests.
- [x] Wire mobile derivatives into responsive scene, story, project, atmosphere, and dossier rendering through picture source media queries.
- [x] Validate mobile derivative loading paths, visual fidelity, and project regression safety. All five uploaded paths return 200, 34 tests pass, TypeScript and production build pass, and no high-severity dependency vulnerabilities are reported.
- [x] Commit, push, and checkpoint the mobile derivative optimization as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the real-phone responsive path for missing images, hidden content, overflow, and breakpoint regressions. Found mobile picture sources using a deployment-unreliable same-origin path while desktop mode used the stable CDN.
- [x] Correct mobile media rendering and responsive composition while preserving all content, links, and desktop behavior. All mobile derivatives now use stable public CDN URLs and retain desktop originals.
- [x] Verify phone, tablet, and desktop previews, asset requests, tests, types, build, and runtime errors. Reviewed 390x844, 768x1024, and 1280x720; 34 tests pass, TypeScript and build pass, asset paths resolve, audit is clear.
- [x] Commit, push, and checkpoint the responsive repair as mantisdarling. Checkpoint follows after source cleanup.
- [x] Add graceful load-aware fade-in transitions for mobile cinematic images on slow connections. Frames stay allocated, selected sources fade in after load, and mobile failures fall back to desktop sources.
- [x] Add an accessible swipeable project carousel for touch-sized screens while preserving the desktop project grid. Mobile uses horizontal scroll snap with a next-card peek; desktop remains the original grid.
- [x] Verify mobile, tablet, desktop, keyboard, reduced-motion, image-error, tests, types, build, and runtime behavior. 36 tests pass, TypeScript and production build pass, dependency audit is clear, and 390px plus 1280px previews were reviewed.
- [x] Commit, push, and checkpoint the mobile interaction upgrade as mantisdarling. Checkpoint follows after source cleanup.
- [x] Establish and audit a device matrix covering small phones, standard phones, tablets, laptops, and wide desktop screens. Reviewed 320x700, 390x844, 768x1024, 1024x900, 1280x720, and 1440x900.
- [x] Refine verified responsive layout, media framing, typography, interaction spacing, and overflow behavior without removing content. Restored project telemetry overlays, hardened touch scrolling, added focus treatment, added an older-engine overflow fallback, and made the small-phone hero height safer.
- [x] Validate visual, accessibility, performance, reduced-motion, tests, types, build, audit, and runtime behavior across the device matrix. 36 tests pass, TypeScript and production build pass, dependency audit reports no known vulnerabilities, diff hygiene passes, and final 320px plus 1440px previews were reviewed.
- [x] Commit, push, and checkpoint the full-device quality pass as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the evidence background crop, overlay, and content measure on real phone widths. Confirmed the subject was too dominant and stretched behind the long mobile record.
- [x] Apply a phone-specific evidence image crop and readable layout while keeping desktop treatment unchanged. Added a phone-only focal crop, reduced saturation, controlled opacity, and a stronger lower readability gradient.
- [x] Validate phone, tablet, desktop, accessibility, tests, types, build, audit, and runtime behavior. Reviewed 390px phone and 1280px desktop captures; 36 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Commit, push, and checkpoint the evidence mobile repair as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the Stack background crop, atmosphere layer, and technology list on real phone widths. Confirmed the duplicated atmosphere and full-section crop competed with the mobile list.
- [x] Apply a phone-specific Stack image crop and readable overlay while keeping desktop treatment unchanged. Added a phone-only focal crop, lower saturation, controlled opacity, readable gradient, and duplicate-layer suppression.
- [x] Validate phone, tablet, desktop, accessibility, tests, types, build, audit, and runtime behavior. Reviewed 390px phone and 1280px desktop captures; 36 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Commit, push, and checkpoint the Stack mobile refinement as mantisdarling. Checkpoint follows after source cleanup.
- [x] Inventory the full application and audit the cross-device matrix from small phones through wide desktop. Reviewed 320x700, 390x844, 768x1024, 1024x900, 1280x720, and 1440x900.
- [x] Audit and fix verified responsive UI, media, accessibility, interaction, and overflow issues without removing content. Restored project visual telemetry, hardened touch behavior, improved mobile evidence and Stack art direction, and fixed dossier image-state reuse.
- [x] Audit code safety, dependency health, performance, data integrity, and deployment boundaries; fix only verified issues. No credential markers or unclassified dangerous browser sinks were found, dependency audit is clear, expected security headers are present, and all portfolio data remains preserved.
- [x] Run complete visual, functional, regression, type, build, audit, and runtime validation across the matrix. 37 tests pass, TypeScript and production build pass, audit reports no known vulnerabilities, diff hygiene passes, and final phone, tablet, and desktop captures were reviewed.
- [x] Commit, push, and checkpoint the complete quality audit as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the client entry, route boundaries, and current production chunk sizes. The original 683 KB main chunk and heavy private Studio route were identified as the primary split targets.
- [x] Implement safe route and dependency code-splitting without changing public behavior. Home, Studio, Privacy, and NotFound now load lazily with a lightweight accessible suspense shell.
- [x] Validate chunk output, initial load, route behavior, responsive UI, tests, types, build, audit, and runtime errors. Entry is now 465 KB, Home 64 KB, Studio 123 KB, Privacy 3.5 KB, and NotFound 4.4 KB; 37 tests pass, TypeScript and build pass, and audit reports no known vulnerabilities.
- [x] Commit, push, and checkpoint the code-splitting improvement as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the lazy route boundary and current navigation lifecycle before adding transitions. Confirmed route modules are independently lazy-loaded and the existing error boundary remains in place.
- [x] Implement a smooth route transition that preserves lazy loading, direct URLs, focus behavior, and reduced motion. Added a keyed route handoff with a short transform and opacity entrance and a blade-line cue, disabled under reduced motion.
- [x] Validate page transitions, route loading, accessibility, responsive behavior, tests, types, build, audit, and runtime errors. Verified Home, Privacy, and Studio routes at 390px; 37 tests pass, TypeScript and build pass, audit is clear, and route requests return successfully.
- [x] Commit, push, and checkpoint the page-transition refinement as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the existing route-transition blade-line cue and crimson visual token. Confirmed the one-pixel fixed line and existing transition timing.
- [x] Add a subtle glow to the blade-line cue without changing timing or layout. Added a compact crimson halo and preserved the existing animation geometry.
- [x] Validate the glow on phone and desktop, including reduced motion, tests, types, build, audit, and runtime behavior. Reviewed 390px and 1440px routes; 37 tests pass, TypeScript and build pass, audit is clear, and diff hygiene passes.
- [x] Commit, push, and checkpoint the blade-line glow refinement as mantisdarling. Checkpoint follows after source cleanup.
- [x] Inventory every image and video source, fallback, responsive derivative, and CDN path used by the portfolio. Audited 17 live CDN media URLs, five mobile derivatives, two MP4s, poster frames, and the brand mark; all returned expected HTTP 200 content types.
- [x] Fix verified media quality, responsive source, crop, poster, loading, or video fallback issues without changing portfolio content. Added seven quality-preserving WebP variants, a responsive mobile closing poster, metadata preload for the closing video, and a first-render mobile viewport guard.
- [x] Validate all media requests and visual fidelity across phone, tablet, laptop, and desktop layouts. Reviewed 390px, 768px, and 1440px full-page captures; video sources are H.264 736x414, poster and WebP paths resolve, and imagery remains present across layouts.
- [x] Run tests, types, production build, audit, runtime checks, commit, push, and checkpoint as mantisdarling. 38 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Audit the mobile scroll lock, project carousel overflow, and viewport behavior causing normal vertical scrolling to stop. The carousel’s pan-x rule was blocking vertical page panning on touch starts over project cards, and the legacy shell fallback could trap overflow.
- [x] Restore vertical page scrolling while preserving horizontal project swiping and modal-only scroll locking. Enabled pan-x pan-y for the carousel, clipped only horizontal body overflow, and kept modal body locking unchanged.
- [x] Validate phone scrolling, carousel interaction, modal behavior, desktop layout, tests, types, build, audit, and runtime errors. Reviewed 390px full-page and 1440px desktop previews; 38 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Commit, push, and checkpoint the scroll repair as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the Stack markup and mobile visual-layer structure causing the artwork to render as a separate block. The mobile picture was constrained to 72svh inside an absolute backdrop, leaving the rest of the section unfilled.
- [x] Move the Stack artwork behind the mobile heading and technology list with a controlled crop and readable overlay while preserving desktop behavior. The picture now fills the section, with the duplicate atmosphere layer still suppressed on phones.
- [x] Validate phone, tablet, desktop, accessibility, tests, types, build, audit, and runtime behavior. Reviewed 390px full-page and 1440px desktop captures; 38 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Commit, push, and checkpoint the Stack mobile composition repair as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the Stack markup and mobile visual-layer structure causing the artwork to appear as a separate block. The mobile picture was constrained to 72svh inside an absolute backdrop, leaving the remaining section without the intended background layer.
- [x] Move the Stack artwork behind the mobile heading and technology list with a controlled crop and readable overlay while preserving desktop behavior. The picture now fills the section, the foreground content is explicitly layered above it, and the duplicate atmosphere remains hidden on phones.
- [x] Validate phone, tablet, desktop, accessibility, tests, types, build, audit, and runtime behavior. Reviewed 390px full-page and 1440px desktop previews; 39 tests pass, TypeScript and build pass, audit reports no known vulnerabilities, and diff hygiene passes.
- [x] Commit, push, and checkpoint the Stack mobile composition repair as mantisdarling. Checkpoint follows after source cleanup.
- [x] Audit the actual 360px phone rendering section by section and identify every remaining image, spacing, overflow, and layer failure. Reviewed 360px, 390px, 768px, and 1440px captures; the Stack issue was the duplicated atmosphere image plus a constrained phone picture treatment.
- [x] Rebuild the verified mobile image and content layers so each section has an intentional phone composition. Removed the duplicate Stack artwork, kept one direct cinematic backdrop, and layered the Stack heading and technology list above it.
- [x] Validate 360px, 390px, tablet, and desktop behavior, including scroll, carousel, media, accessibility, tests, types, build, audit, and runtime errors. 40 tests passed, TypeScript passed, production build passed, high-severity dependency audit found no known vulnerabilities, and 360px, 390px, 768px, and 1440px previews were reviewed.
- [x] Commit, push, and checkpoint the verified phone repair as mantisdarling. Commit bb87ea1 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Recheck the complete portfolio device by device at exact phone, tablet, and desktop breakpoints, including media, layering, spacing, overflow, scrolling, and navigation. Reviewed 320px, 360px, 390px, 412px, 768px, 1024px, 1280px, and 1440px full-page previews.
- [x] Fix any verified cross-device regressions without removing portfolio data or changing backend behavior. No cross-device visual regressions were found; the single verified repository issue was the missing security scan utility, which was repaired without changing application behavior.
- [x] Re-run automated tests, TypeScript, production build, dependency audit, and runtime console checks after the device audit. Tests, TypeScript, build, dependency audit, secret scan, and diff checks passed; no new browser error-level records or network failures appeared during the current review.
- [x] Save and report the cross-device verification result as mantisdarling. The device audit and validation utilities are ready for checkpointing.
- [x] Repair the missing security:secrets validation script discovered during the cross-device quality gate and verify it without exposing credentials. Added a conservative redacted-pattern scanner and a regression test; the scan passed for 184 tracked files.

- [x] Fix the mobile Work chapter where the desktop project grid is overflowing the phone viewport and making the first card dominate the composition. Rebuilt the phone archive as compact snap cards with controlled media ratios and no desktop-width overflow.
- [x] Preserve every project, link, metadata field, and card interaction while recomposing the Work archive for phone widths. All project records remain present, touch targets stay active, and the desktop grid rules are unchanged.
- [x] Validate the Work fix at 320px, 360px, 390px, 412px, tablet, and desktop widths, then rerun tests, types, build, audit, and runtime checks. Reviewed 320px, 360px, 390px, 412px, 768px, and 1440px captures; 41 tests passed, TypeScript and build passed, audit was clear, and current runtime logs showed no new errors or network failures.
- [x] Commit, push, and checkpoint the mobile Work repair as mantisdarling. Commit bae225a was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Add mobile project carousel position indicators that clearly communicate the current slide and available projects. Added a live 01 / total counter and a visual active-segment rail.
- [x] Keep the indicators accessible, responsive, and hidden from the desktop layout without changing project data or touch scrolling. The counter uses a polite live region, the rail is mobile-only, and all project records and touch behavior remain unchanged.
- [x] Validate indicator updates on mobile and absence on larger screens, then rerun tests, types, build, audit, and runtime checks. Verified 390px mobile and 1440px desktop captures; 41 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the carousel indicator enhancement as mantisdarling. Commit 3efc585 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Recompose the Evidence chapter on phones so dense records, dossier groups, and the artwork do not read as one overloaded vertical field. Superseded by the owner’s narrower request to close all dossiers by default; no broad layout rewrite was applied.
- [x] Preserve every education, credential, course, community, research, competition, language, interest, and vision record while improving phone hierarchy and readability. The accordion-only scope preserved all records without changing their content.
- [x] Validate Evidence at 320px, 360px, 390px, 412px, tablet, and desktop widths, then rerun tests, types, build, audit, and runtime checks. The approved accordion-only scope was verified at 390px and 1440px, with the complete automated and build gates passing.
- [x] Commit, push, and checkpoint the Evidence phone repair as mantisdarling. Scope narrowed to the requested closed-by-default behavior; the accordion fix was committed and pushed as mantisdarling.

- [x] Make every Evidence accordion closed on initial page load while preserving each arrow toggle and all existing records. Removed the initial open attribute from the Courses and learning group; all four dossiers now start collapsed.
- [x] Add regression coverage for the initial closed state and open-close behavior. The focused Home suite confirms four closed dossiers, retained course content, and native summary open-close behavior.
- [x] Validate the Evidence disclosure behavior on phone and desktop, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the Evidence accordion fix as mantisdarling. Commit 39c7ee1 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Add smooth height-aware transitions when Evidence accordions open and close, keeping every group closed by default. Added measured max-height, opacity, visibility, and arrow rotation transitions.
- [x] Preserve native summary toggle semantics, focus behavior, content accessibility, and reduced-motion preferences during the transition. Controlled buttons expose expanded state and panel relationships, hidden panels are inert, and reduced motion disables transitions.
- [x] Validate the accordion animation on phone and desktop, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the accordion transition enhancement as mantisdarling. Commit 70dd817 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Change Evidence accordion header text color on hover without changing the closed state, arrow behavior, focus styling, or content. Pointer-capable devices now transition the header label to Mantis red while touch and keyboard behavior remain unchanged.
- [x] Validate the hover refinement across mobile and desktop and rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the accordion hover-color refinement as mantisdarling. Commit 9d71c90 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Add a smooth 180-degree rotation to each Evidence accordion arrow when its section opens and closes. The closed state is explicit at 0 degrees and the open state rotates to 180 degrees with a 320ms transform transition.
- [x] Preserve the current hover color, focus state, closed-by-default behavior, and reduced-motion fallback. The transform is centered and GPU-friendly, while reduced-motion rules disable the transition.
- [x] Validate the arrow rotation on phone and desktop, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the arrow rotation enhancement as mantisdarling. Commit 415eb68 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Add a subtle fade-in to Evidence accordion content when a section opens without changing the height transition or content. Added a 240ms opacity fade with a restrained 0.35rem translate-in and 70ms delay.
- [x] Preserve the arrow rotation, closed-by-default state, accessible hidden state, and reduced-motion fallback. Existing panel height and arrow motion remain intact, hidden panels stay inert, and reduced motion removes the content transition.
- [x] Validate the combined accordion motion on phone and desktop, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the accordion content fade-in as mantisdarling. Commit 5b1a260 was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Optimize Evidence accordion animations for physical iPhone and Android performance without changing behavior or content. Stable content nodes now use compositor-safe opacity and translate3d motion, while phone timing is shorter and less demanding.
- [x] Reduce unnecessary layout reads, observer updates, and paint work while preserving accessible disclosure and reduced-motion behavior. ResizeObserver reads contentRect, updates are batched through requestAnimationFrame, unchanged heights are ignored, paint is contained, and cleanup cancels pending frames.
- [x] Validate the optimized motion at phone and desktop breakpoints, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 42 tests, TypeScript, production build, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the mobile accordion performance optimization as mantisdarling. Commit 2e2a65a was authored by mantisdarling and pushed to mantisdarling/Mhini main; checkpoint follows.

- [x] Recheck the full portfolio code, responsive rules, data integrity, media layers, accessibility, and performance behavior across all supported devices. Audited the React page, responsive stylesheet, tests, project data usage, media references, disclosure state, animation cleanup, and server-facing behavior.
- [x] Run fresh visual and interaction checks at narrow phones, standard phones, tablets, small desktop, and wide desktop breakpoints. Reviewed 320px, 360px, 390px, 412px, 768px, 1024px, 1280px, and 1440px full-page renders; no visible clipping, horizontal overflow, broken background layer, or responsive regression was found.
- [x] Fix only verified regressions, then rerun tests, types, production build, dependency audit, secret scan, diff checks, and runtime log review. No application regression required changes; the only static match was the existing chart CSS helper, and all quality gates passed with no new browser or network failures.
- [x] Commit, push, and checkpoint the complete regression audit as mantisdarling. Commit follows after this verified audit record.

- [x] Inventory the repository, routes, data model, deployment boundaries, security controls, media pipeline, and existing CI checks before making broad changes. Confirmed the public and admin tRPC surface, MySQL or Supabase adapter, cache policy, recovery routes, Vercel headers and rewrites, media helpers, 7 local projects, and 42-test baseline.
- [x] Polish the codebase by removing verified unnecessary code, consolidating safe duplication, and preserving all existing behavior and portfolio data. Removed the unused dossier ref, added one shared safe URL helper, bounded normalized tags, and kept generated Vercel functions synchronized.
- [x] Improve real-device performance for iPhone and Android, including media loading, animation cost, layout stability, and resilient fallbacks. Prioritized the hero fallback image, preserved responsive mobile sources, added public read caching, and removed the content-visibility optimization after it caused blank full-page chapters.
- [x] Strengthen project case-study presentation with clearer problem, contribution, technical decision, outcome, and link structure using existing data only. Existing role, problem, solution, description, highlights, tags, and links are now represented in the dossier flow, with role surfaced in archive metadata and accessible modal relationships.
- [x] Improve SEO and social sharing metadata without adding inaccurate claims or changing the visible identity. Added canonical, description, robots, Open Graph, Twitter card, theme, favicon, preconnect, robots.txt, sitemap.xml, and verified Bluesky, Instagram, and Google Developer links.
- [x] Harden verified application security boundaries and document what can and cannot be proven for a 50,000-user workload using safe local or staging checks only. Added HTTP(S)-only external link validation, bounded public tags, a shared public-read cache policy, and `docs/SECURITY-SCALE-REVIEW.md`; no intrusive or production load test was performed.
- [x] Run the complete regression, accessibility, performance, build, dependency, secret, and runtime quality gates. 46 tests passed, TypeScript passed, both production and Vercel builds passed, dependency and secret audits passed, diff checks passed, and current browser and network logs showed no new failures.
- [x] Commit, push, and checkpoint the repository polish as mantisdarling. Commit d55776a was created with mantisdarling as author. The managed project remote rejected direct shell push because its credentials are checkpoint-only; the recoverable project state is being saved through the project checkpoint flow.
- [x] Remove or revise the offscreen content-visibility optimization after the fresh 390px capture exposed blank distant chapters in full-page rendering; preserve stable layout and media performance with safer techniques. Removed the rule and reverified continuous 360px, 390px, 768px, and 1440px renders.

- [x] Add accessible social sharing controls to each project dossier using existing project title, description, and verified links. Added Share and Copy link controls plus X and LinkedIn fallbacks using the existing project metadata.
- [x] Support browser-native sharing when available plus safe copy-link and verified platform fallback actions without tracking or new dependencies. Native Web Share is preferred, Clipboard is used for copy, and fallback links are HTTP(S)-only, opener-safe, and dependency-free.
- [x] Validate sharing controls on mobile and desktop, including keyboard access, safe URL handling, and responsive layout, then rerun tests, types, build, audit, and runtime checks. Verified 390px and 1440px captures; 47 tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, and current runtime logs passed.
- [x] Commit, push, and checkpoint the project sharing enhancement as mantisdarling. Commit f2ea6cf was authored by mantisdarling; the managed project checkpoint will preserve the verified feature state.
- [x] Enhance copy-link feedback with a subtle toast notification and animation, including accessible live status, responsive placement, and reduced-motion support. Added a temporary status toast with compositor-safe entrance and exit motion, mobile placement, and reduced-motion behavior.
- [x] Validate the toast interaction with focused tests, full tests, type checking, builds, audits, and a responsive preview before checkpointing. Verified 19 focused Home tests, 48 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, and 390px and 1440px previews with clean browser and network logs.
- [x] Add a subtle pointer hover treatment and accessible tooltip to the copy-link button, with keyboard parity and reduced-motion support. Added a red edge cue, tooltip wrapper, keyboard focus parity, and reduced-motion fallback.
- [x] Validate the copy-link hover and tooltip behavior, then rerun tests, builds, audits, and responsive previews before checkpointing. Verified 19 focused Home tests, 48 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, and 390px and 1440px previews with clean browser and network logs.
- [x] Audit all visible portfolio text for alignment, wrapping, spacing, hierarchy, contrast, and accidental copy faults across responsive breakpoints without changing authoritative content. Reviewed the hero, profile, story, work, stack, evidence, finale, contact, modal, navigation, and footer copy against the authored data.
- [x] Correct verified text-layout faults in the portfolio shell, chapters, project cards, dossiers, navigation, and footer while preserving the Mantis visual identity and interaction behavior. Added bounded text widths, balanced headings, stable flex and grid shrink behavior, intentional metadata alignment, long-word wrapping, readable mobile stacking, and consistent footer and contact alignment.
- [x] Validate the text-alignment pass with focused regressions, full tests, types, builds, audits, responsive screenshots, and runtime checks before checkpointing. Verified 20 focused Home tests, 49 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, 320px, 390px, 768px, and 1440px previews, and clean browser and network logs.
- [x] Add a subtle one-time fade-in for selected portfolio text blocks as they enter the viewport, using compositor-safe motion and preserving content order. Added one-time IntersectionObserver reveals for hero, chapter, story, project, stack, evidence, finale, contact, and footer text groups with a restrained upward fade.
- [x] Add reduced-motion and unsupported-observer fallbacks plus focused regression coverage for the scroll reveal. Unsupported observers leave content visible, reduced-motion users bypass hiding and animation, and hook tests verify reveal, unobserve, and cleanup behavior.
- [x] Validate the scroll reveal across responsive previews, motion behavior, tests, types, builds, audits, and runtime checks before checkpointing. Verified 23 focused tests, 52 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, clean browser and network logs, and 390px and 1440px previews.
- [x] Audit the complete portfolio across 320px, 390px, 768px, 1024px, 1280px, and 1440px layouts for spacing, typography, media framing, overflow, controls, contrast, and motion regressions. Captured and reviewed all six target widths with no visible clipping, horizontal overflow, or section handoff failure.
- [x] Implement verified cross-device refinements without removing authored content or weakening existing sharing, accordion, carousel, security, and reduced-motion behavior. Added safe anchor offsets, 44px touch targets, mobile spacing normalization, stable contact and footer copy wrapping, and tablet padding controls.
- [x] Verify touch, keyboard, modal, carousel, accordion, scroll-reveal, media, accessibility, and fallback behavior across the responsive layouts. Focused interaction tests passed, including the existing sharing, modal, carousel, accordion, and scroll-reveal coverage; responsive visual checks passed at 320px, 390px, 768px, 1024px, 1280px, and 1440px.
- [x] Run the full tests, type checks, production and Vercel builds, dependency and secret audits, diff hygiene, runtime checks, and responsive previews before checkpointing. Verified 52 tests, TypeScript, production and Vercel builds, no known production dependency vulnerabilities, secret scan, clean diff checks, clean browser/network error scans, and responsive previews.
- [x] Recompose the Stack chapter so the existing artwork is the background behind the heading, description, and technology controls on desktop and tablet while preserving the working phone composition. The desktop and tablet chapter now uses one continuous image field behind the foreground content, while phone keeps its approved crop.
- [x] Correct the Stack chapter’s visible text alignment and responsive spacing before changing its artwork source. The heading, supporting copy, and technology list now share a bounded reading column with intentional tablet stacking and stable row padding.
- [x] Replace the Stack backdrop with the attached monochrome mountain and waterfall artwork, keeping one image field behind the foreground content and using a responsive source safely. The uploaded artwork is the desktop source; the existing phone-specific blade crop remains the mobile source.
- [x] Verify the Stack background layering, contrast, accordion behavior, media loading, and responsive spacing at phone, tablet, and desktop widths, then run the release gate before checkpointing. Verified 21 focused Home tests, 52 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, clean runtime error scans, and 1080px, 768px, and 390px previews.
- [x] Refine the Stack “Tools are judgment” heading line height and display weight on desktop screens without changing the mobile composition or authored copy. Added a desktop-only heavier display weight, tighter line height, closer tracking, and restrained separation shadow; phone typography remains unchanged.
- [x] Validate the heading refinement across desktop, tablet, and mobile previews plus tests, types, builds, audits, and runtime checks before checkpointing. Verified 21 focused Home tests, 52 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, diff checks, clean runtime scans, and 1080px and 390px previews.
- [x] Inventory the repository’s frontend, backend, configuration, dependency, secret, test, storage, auth, and deployment boundaries without changing production data or credentials. Reviewed the active runtime, Vercel function, tRPC procedures, MySQL and Supabase adapters, storage proxy, recovery handlers, deployment policy, dependency manifest and lockfile, tests, and recent runtime logs.
- [x] Run static, dependency, secret, runtime, and bounded security inspections and record verified findings with severity, affected files, and evidence. Findings and residual limitations are documented in `docs/SECURITY-AUDIT-2026-08.md`; no production data, credentials, or third-party infrastructure were modified or attacked.
- [x] Fix verified correctness and safety findings while preserving public portfolio behavior, authored content, authentication boundaries, storage references, and existing responsive interactions. Fixed Supabase owner-admin parity, aligned Vercel CSP, hardened storage keys and error logging, and preserved existing runtime behavior.
- [x] Remove only demonstrably unused code or duplicate implementation and simplify maintainability hotspots without weakening coverage. Removed the unreferenced `server/index.ts` bootstrap and unused direct dependencies `body-parser`, `lodash`, `lodash-es`, and `mdast-util-to-hast`; active entrypoints and used UI dependencies remain.
- [x] Re-run the full regression, type, build, dependency, secret, runtime, and bounded scale checks and document remaining provider-level limitations. Verified 62 tests across 17 files, TypeScript, production and Vercel builds, no known high-severity production dependency vulnerabilities, secret scan, diff checks, clean browser/network scans, and documented the residual Recharts deprecation and provider-level capacity limitations.
- [x] Redact provider response bodies and raw error objects from storage and recovery logs, with regression coverage for generic public errors and non-sensitive operational logging. Storage proxy logs now record status only; recovery logs record only an error category; public responses remain generic and existing boundary tests pass.
- [x] Inventory fetched portfolio data boundaries and existing skeleton or loading components before adding new states. Reviewed the public Work query, private project-console query, existing auth skeletons, and shared skeleton primitive.
- [x] Implement stable, accessible skeleton states for data sections with restrained animation, responsive geometry, and reduced-motion support. Added public project-card placeholders, private project-list rows, live loading announcements, `aria-busy`, reserved geometry, calm shimmer motion, mobile adjustments, and reduced-motion fallbacks.
- [x] Add deterministic loading-state regression coverage and validate loaded-state handoffs, responsive previews, tests, types, builds, audits, and runtime checks before checkpointing. Verified 25 focused skeleton/Home/reveal tests, 64 total tests across 18 files, TypeScript, production and Vercel builds, dependency and secret audits, diff checks, source safety scan, clean browser/network scans, and 390px, 768px, and 1440px previews.
- [x] Audit the current portfolio experience at phone, tablet, desktop, and wide desktop widths and prioritize only high-impact refinements that fit the Mantis visual system. Reviewed current 390px, 768px, and 1440px compositions and selected orientation and control affordances rather than adding decorative noise.
- [x] Add a scroll-aware navigation state and subtle section transition polish to improve orientation without adding visual noise or changing authored content. Navigation now tracks the visible chapter with a restrained Mantis Red rule and keeps the mobile menu relationship explicit.
- [x] Strengthen keyboard and touch affordances for the navigation and primary interactive controls while preserving existing modal, carousel, accordion, sharing, skeleton, and reduced-motion behavior. Added aria-current location state, aria-controls menu semantics, visible keyboard parity, and compositor-safe press feedback with reduced-motion fallback.
- [x] Run the full visual, responsive, regression, type, build, dependency, secret, runtime, and diff quality gates before checkpointing the selected improvements. Verified 26 focused tests, 65 total tests across 18 files, TypeScript, production and Vercel builds, dependency audit, secret scan, source safety scan, diff checks, clean browser/network scans, and 390px, 768px, and 1440px previews.
- [x] Audit remaining storytelling hierarchy, project context, chapter handoffs, media captions, mobile navigation feedback, and dense spacing for high-impact improvement opportunities. Reviewed current phone and wide desktop compositions and selected restrained orientation, caption readability, and keyboard clarity improvements.
- [x] Improve project and chapter clarity without changing or removing authored content, links, media, or existing safety boundaries. Added clearer story-caption separation and keyboard entry emphasis for project cards while preserving every authored record, link, and media source.
- [x] Refine responsive motion, captions, navigation feedback, touch affordances, and reduced-motion fallbacks without introducing layout shifts. Added a skip link, Escape dismissal for menus and dossiers, stable focus styling, balanced story captions, and reduced-motion-safe transitions.
- [x] Run the complete visual, regression, accessibility, security, type, build, dependency, runtime, and diff quality gates before checkpointing the second polish pass. Verified 27 focused tests, 66 total tests across 18 files, TypeScript, production and Vercel builds, dependency audit, secret scan, source safety scan, diff checks, clean browser/network scans, and 390px and 1440px previews.
- [x] Inspect all cursor, custom pointer, pointer-events, hover, and media-query rules to identify why the native laptop cursor is not visible. The root cause was a legacy fine-pointer `cursor: none` rule remaining in the global stylesheet after the custom cursor layer was no longer active.
- [x] Restore a visible native cursor for fine-pointer devices while preserving touch behavior, custom hover cues, keyboard focus, and reduced-motion fallbacks. Added a scoped fine-pointer override that restores the native cursor and pointer cursors for links, buttons, and role buttons; touch behavior is unchanged.
- [x] Validate cursor behavior and rerun focused tests, full tests, types, builds, audits, runtime checks, and responsive previews before checkpointing. Verified 27 focused tests, 66 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, source safety scan, diff checks, clean browser/network scans, and 1440px and 390px previews.
- [x] Inspect project-card structure, image treatment, focus behavior, and existing hover rules before applying the interaction refinement. Confirmed cards are semantic keyboard-activatable articles with existing dossier cues and mobile carousel behavior.
- [x] Enhance project-card hover and focus cues with pointer-device-only depth, image motion, dossier affordance, and reduced-motion fallback without layout shifts. Added a restrained translate3d lift, shallow shadow, inner edge cue, image scale, dossier motion, touch reset, and reduced-motion fallback.
- [x] Validate project-card hover, focus, touch, responsive, and reduced-motion behavior, then run tests, types, builds, audits, runtime checks, and responsive previews before checkpointing. Verified 27 focused tests, 66 total tests, TypeScript, production and Vercel builds, dependency audit, secret scan, source safety scan, diff checks, clean browser/network scans, and 1440px and 390px previews.
- [x] Inventory active frontend, backend, auth, storage, database, deployment, recovery, logging, dependency, and CI boundaries for the fresh hardening review. Reviewed application, adapters, deployment, recovery, logging, dependency, secret, and CI surfaces without modifying production data.
- [x] Run non-invasive static, dependency, secret, runtime, resilience, and bounded scale audits and record verified findings with evidence and severity. High-severity dependency audit, secret scan, source safety scan, runtime log review, and a bounded 200-request local smoke test passed.
- [x] Fix verified vulnerabilities, error paths, crash risks, and data-integrity issues without changing production data or weakening existing user-facing behavior. Added shared upstream deadlines and retained HTTP(S)-only URL validation with focused regressions.
- [x] Remove only demonstrably unnecessary code and strengthen deterministic quality and security gates without hiding findings through broad allowlists. Prior dead-code and dependency cleanup remained intact; no new speculative removal was made.
- [x] Re-run bounded scale, resilience, regression, type, build, audit, runtime, and diff checks and document what remains provider-dependent. Full tests, TypeScript, production and Vercel builds, audits, diff checks, runtime review, and local smoke validation passed; provider limitations remain documented.
- [x] Bound Supabase and independent-auth upstream requests with a shared timeout policy, preserving caller cancellation and validating timeout behavior deterministically.
- [x] Inventory tracked source, generated entrypoints, dependencies, and tests for genuinely unused or unnecessarily verbose code. Confirmed three unused direct dependencies and four unreferenced template files.
- [x] Remove or simplify only verified non-relevant code while preserving behavior, security boundaries, authored content, and API contracts. Removed only the verified unused dependencies and four unreachable template files.
- [x] Add or update focused regressions for cleanup-sensitive behavior and run complete quality gates. Existing regressions plus 69 passing tests, TypeScript, production build, Vercel build, and diff checks passed.
- [x] Save the cleaned repository checkpoint under mantisdarling and document remaining intentional framework or generated code. Intentional framework helpers, active UI primitives, generated Vercel entrypoints, and test support remain documented and retained.
- [x] Stabilize the public-feed integration regression under concurrent test execution without changing production request deadlines or masking application failures. Mocked only the project-data seam for the cache-header assertion; the full suite passed.
- [x] Inventory active UI modules, exports, tests, and CI workflow surfaces for safe modularization and unused-export enforcement. Home was the largest active UI page at 559 lines before extraction; CI had no existing workflow.
- [x] Add a deterministic unused-export detection command and CI workflow gate with documented false-positive handling. Added ts-prune, a narrow wrapper, package script, GitHub Actions workflow, and docs/QUALITY-GATES.md.
- [x] Split the largest active UI module or modules into behavior-focused components without changing rendered content, interactions, or responsive behavior. Home now composes model data and reusable media, card, sharing, story, and dossier behavior; the page module dropped from 559 to 234 lines.
- [x] Run full tests, types, builds, security checks, CI checks, runtime checks, and responsive previews across phone, tablet, and desktop widths. 69 tests passed; type, production, Vercel, dependency, secret, formatting, diff, runtime, and 390px, 768px, and 1440px preview checks passed.
- [x] Save the verified CI and UI modularization checkpoint under mantisdarling and document any intentional exports. Intentional framework, contract, generated, and test-support surfaces are documented in docs/QUALITY-GATES.md.
- [x] Inventory the split portfolio components and current loading, media-failure, route, and error-boundary paths. Reused the existing route boundary and added a local portfolio boundary plus media load-state handling.
- [x] Add smooth reduced-motion-safe loading animations and component-level error recovery around the split portfolio modules. Added shimmer-backed image shells, fade-in recovery UI, retry behavior, and reduced-motion fallbacks.
- [x] Add focused regressions for loading, media failure, and component recovery behavior. Added a jsdom recovery test and preserved the existing Home loading and media regressions.
- [x] Run full tests, types, builds, security checks, runtime checks, and responsive previews across phone, tablet, and desktop widths. 70 tests passed; all quality gates and 390px, 768px, and 1440px previews passed with clean runtime logs.
- [x] Save the verified loading and recovery checkpoint under mantisdarling and document the behavior. Shimmer-backed media loading, local retry boundaries, reduced-motion fallbacks, and responsive verification are complete.
- [x] Run a fresh non-invasive health check covering tests, unused exports, types, builds, dependencies, secrets, runtime logs, and responsive previews. 70 tests passed; unused-export, TypeScript, production and Vercel builds, audit, secret, formatting, diff, and runtime checks passed.
- [x] Record the verified health result and any provider-dependent limitations without changing application behavior. Desktop, tablet, and phone previews rendered; no new runtime errors were found.
- [x] Fix the verified 768px Studio layout overlap where the persistent sidebar obscures the main console content; preserve desktop and phone behavior and re-run all health checks. The inner Studio shell now participates in horizontal flex layout, and the corrected tablet preview plus complete gates passed.
- [x] Audit the public portfolio, Studio, privacy route, media, navigation, consent UI, and interactive states at phone, tablet, laptop, desktop, keyboard, and reduced-motion conditions. Reviewed 320px, 390px, 768px, 1024px, and 1440px route previews plus full-page public layouts and runtime logs.
- [x] Fix every verified cross-device layout, overflow, contrast, media, interaction, accessibility, or runtime issue without removing authored content. Corrected the verified 768px Studio sidebar overlap by making the inner shell participate in horizontal flex layout; no other verified defect required a code change.
- [x] Re-run complete tests, unused-export, type, security, dependency, build, runtime, and responsive checks after the device fixes. 70 tests passed and all listed gates plus corrected 768px and full-page 390px and 1440px previews passed.
- [x] Save the verified cross-device regression checkpoint under mantisdarling and document remaining limitations. Physical-device behavior, network quality, and provider uptime remain outside sandbox verification.
- [x] Compare the current working tree and checkpoint against the prior verified state for behavioral, layout, content, and dependency regressions. No application-code drift was found beyond the already verified Studio flex correction and loading or recovery additions.
- [x] Exercise critical public, Studio, privacy, auth, media, loading, recovery, navigation, keyboard, consent, reduced-motion, and responsive paths. Route previews and runtime probes covered the public, Studio, privacy, health, readiness, auth, and public-feed paths across 320px through 1440px.
- [x] Re-audit security boundaries, secrets, dependencies, headers, authorization, storage, upstream deadlines, error redaction, and failure handling. Source-safety, secret, dependency, header, auth-boundary, and runtime scans passed; the only unsafe-looking chart sink is the existing controlled style serializer.
- [x] Fix every verified regression and rerun the complete automated and device verification suite. The 768px Studio overlap was already corrected; the current regression pass found no new application defect. 70 tests and all automated gates passed.
- [x] Save the verified regression and security audit checkpoint under mantisdarling and document evidence and limitations. Correct health probes, clean runtime logs, full automated gates, and cross-device previews are recorded.
- [x] Restore and verify the repository-local Git author identity as mantisdarling after the regression audit found Manus in the active local config. Verified name mantisdarling and noreply email locally.
- [x] Inspect the app shell, asset paths, deployment routing, and existing tests for service-worker compatibility. Confirmed production Vite output, Vercel rewrites, API boundaries, and client bootstrap behavior.
- [x] Implement a versioned service worker with offline fallback, safe static caching, network-first navigation, and no caching of auth or dynamic API responses. API, auth, non-GET, and cross-origin requests bypass the worker.
- [x] Register the service worker safely in the client and add deterministic cache-policy and offline regressions. Added production-only registration, a CI policy checker, and a no-cache deployment rule for sw.js.
- [x] Run complete tests, types, unused-export, security, dependency, build, runtime, and responsive checks. 70 tests passed; all configured gates and service-worker runtime and packaging probes passed.
- [x] Save the verified offline-support checkpoint under mantisdarling and document browser and provider limitations. Physical browser offline behavior and provider availability remain outside sandbox verification.
- [x] Run a bounded local capacity and resilience simulation across health, readiness, auth, public-feed, page, and static service-worker paths with controlled concurrency. Warm 100-worker and 500-worker bursts each completed 1,800 of 1,800 read-only requests successfully.
- [x] Measure success rate, latency percentiles, 5xx responses, process stability, resource behavior, and security headers, then document the evidence and the limits of extrapolating to 50,000 users. Results and limits are recorded in docs/CAPACITY-TEST-2026-08.md; the 500-worker run had 448 requests/s, p50 309 ms, p95 3.30 s, max 3.56 s, zero failures, and all four security headers.
- [x] Coalesce concurrent public project-feed reads so a cold cache does not create an upstream request per visitor during a burst; preserve one-minute freshness and failure behavior. Added a shared in-flight request promise while retaining the one-minute cache.
- [x] Re-run the capacity simulation and full quality gates after the capacity fix, documenting the bounded evidence and production load-test limits. 71 tests, type, build, audit, secret, formatting, diff, runtime, and warm 100-worker capacity gates passed; the higher 500-worker burst also passed.
- [x] Review the split portfolio model, primitives, boundaries, loading behavior, and Home composition for correctness and maintainability. No concrete portfolio-component regression was found; existing loading, recovery, interaction, and responsive boundaries remain intact.
- [x] Review service-worker lifecycle, cache policy, offline fallback, registration, deployment headers, and security boundaries. Found and corrected storage-proxy media caching risk and cache-version migration risk.
- [x] Fix verified review findings and add focused regressions without changing authored content or API contracts. Storage-proxy paths now bypass the worker, cache version is v2, and the CI policy check enforces both protections.
- [x] Run tests, unused-export, types, dependency and secret scans, builds, runtime probes, and responsive previews after the review. 71 tests and all configured gates passed; capacity smoke remained 100% successful at 1,800 requests.
- [x] Save the reviewed portfolio and service-worker checkpoint under mantisdarling and document findings and limitations. Physical browser cache migration and offline behavior remain best verified on a real device after publishing.
- [x] Exclude same-origin storage-proxy media from service-worker caching so private or authorization-sensitive assets are never persisted by the offline layer. The worker bypasses /manus-storage/ and the CI policy checker enforces the marker.
- [x] Bump the service-worker cache version with the privacy policy fix so activation removes any cache created by the previous worker version. The current worker uses mantis-shell-v2 and removes prior mantis-shell caches on activation.
- [x] Inventory the current repository, deployment files, trust boundaries, changed files, tests, and generated artifacts for the urgent audit. Reviewed active source, generated Vercel files, CI, service worker, routes, adapters, storage, recovery, and runtime logs.
- [x] Run non-destructive dependency, secret, source-safety, auth, authorization, storage, request, header, service-worker, recovery, and CI scans. All configured scans passed; malformed-input and CORS probes were safe.
- [x] Exercise safe malformed-input, unauthenticated, cache, offline, runtime, and responsive regression paths without attacking production or changing live data. Verified 320px through 1440px previews, route boundaries, API 404s, storage traversal rejection, auth, consent, media, and service-worker delivery.
- [x] Fix verified vulnerabilities or regressions, rerun all quality gates, and document evidence, failed checks, and provider-dependent limits. Fixed multi-encoded storage traversal, SPA API fallback, tablet Studio form readability, and development API-body logging; the cold-start capacity attempt and its warm rerun are documented.
- [x] Prepare judge-ready device verification and a concise list of high-impact improvements. Desktop, tablet, and phone previews passed; practical judge-day recommendations remain in the final report.
- [x] Save the final audited checkpoint under mantisdarling and record the remaining limitations. Physical-device, live-provider, production-quota, and 50,000-user proof remain outside this safe sandbox audit. Checkpoint: b6cbc5c1.
- [x] Reject multi-encoded traversal and control-character variants before storage-provider presigning, while preserving valid storage keys. Added bounded repeated decoding and focused boundary coverage.
- [x] Return a machine-readable 404 for unknown /api paths instead of allowing the SPA fallback to return HTML with status 200. Added an application-level JSON 404 boundary and regression assertion.
- [x] Add focused regression tests for both boundary fixes and rerun the complete urgent audit gates. 71 tests and all final gates passed.
- [x] Fix the verified 768px Studio console readability regression by switching the main Studio form and action layout to a single-column flow until enough width remains beside the sidebar.
- [x] Prevent the development debug collector from recording same-origin API response bodies or authentication payloads in local network logs; preserve timing, status, and failure metadata only. Added Fetch and XHR redaction plus a CI policy gate.
- [x] Add a semantic, keyboard-accessible chapter-progress rail that tracks the active cinematic section, supports direct navigation, and remains restrained across responsive breakpoints. Added a portaled desktop spine, mobile dock above consent controls, shared chapter metadata, active-location semantics, direct smooth navigation, and responsive visual verification.
- [x] Add a constrained hero-only GPU ink or telemetry field with static poster fallback, reduced-motion and data-saver safeguards, coarse-pointer and low-capability fallbacks, and no new backend or media dependencies. Added a low-power WebGL telemetry canvas limited to the hero, capped DPR, RAF cleanup, WebGL/ResizeObserver fallback handling, and Home regression coverage.
- [x] Refine the hero-only telemetry field with a subtle fine-pointer response, development-only FPS monitoring, and a smooth reduced-motion fallback fade without changing the hero media contract. Added damped pointer interpolation, a development-only readout, state-driven fade fallback, and corrected the cinematic child-layer conflict found during visual verification.
- [x] Synchronize all necessary current website code to GitHub main with mantisdarling author and committer metadata, then verify the remote branch contains the latest checkpoint state. GitHub main now contains final commit 2454374, verified directly with mantisdarling author and committer metadata.
- [x] Add an accessible footer Copy Link button that copies the current website URL using the existing safe feedback/toast behavior and remains usable on mobile. Added reusable clipboard handling, accessible status feedback, tooltip title, focus styling, responsive layout, and a 27-test Home regression.
- [x] Enhance the footer Copy Link success feedback with a polished accessible toast notification that animates in, auto-dismisses, and remains clear on mobile and reduced-motion settings. Added a compact success chip, Mantis Red confirmation marker, distinct fallback state, compositor-safe transform/opacity motion, and reduced-motion handling.
- [x] Push any remaining website code changes to GitHub main with mantisdarling author and committer metadata, then verify the remote branch is synchronized. GitHub main accepted commit a3b4eab, verified directly with mantisdarling author and committer metadata.
- [x] Fix chapter-rail placement across phone, tablet, and desktop layouts so it does not collide with section content, and remove the decorative grid-line overlay that creates unwanted vertical boxes. Removed the hero grid element and line treatment, added coarse-pointer dock rules for phone desktop mode, and verified 390px, 768px, and 1440px compositions.
- [x] Ensure the public website contains no visible FPS, WebGL performance, or development-monitor label while retaining the safeguarded hero effect. Removed the readout markup, timing code, dead CSS, and updated regression assertions; the hero effect remains safely active or falls back without public monitoring text.
- [x] Push the final production-ready website state to GitHub main as mantisdarling, verify the remote commit, and prepare the final checkpoint for user publishing through the project UI. GitHub main accepted commit 13e4fe5, verified directly with mantisdarling author and committer metadata.
- [x] Synchronize any remaining final website code to GitHub main with mantisdarling author and committer metadata, without redeploying the already-live Vercel site. GitHub main accepted commit 034d36f, verified directly with mantisdarling author and committer metadata; Vercel was not redeployed.
- [x] Create a professional README.md for the GitHub repository with accurate project overview, features, architecture, security, performance, setup, validation, and deployment documentation. Added 203 lines of verified documentation and passed formatting, link, command, and diff validation.
- [x] Create a custom cinematic 404 page matching the Mantis theme, with accessible recovery navigation and responsive layout. Added cinematic error copy, Mantis Red recovery action, route-control details, focus-visible styling, reduced-motion behavior, and safe mobile wrapping verified at 390px and desktop preview.
- [x] Fix closing and contact typography alignment, responsive wrapping, and footer overflow across phone, tablet, and desktop layouts without removing authored content. Added bounded headline and copy widths, safe wrapping for long contact strings, and a stable mobile footer flow.
- [x] Audit and correct text alignment, clipping, line wrapping, width constraints, and spacing across every major portfolio section and responsive breakpoint. Added final responsive type guardrails and verified 390px and 1440px full-page previews plus 27 Home tests, TypeScript, formatting, and diff hygiene.
- [x] Push the latest typography/alignment fixes and cinematic 404 page to GitHub main with mantisdarling author and committer metadata, then verify the remote commit. GitHub main accepted commit f4bedc7, verified directly with mantisdarling author and committer metadata.
- [x] Audit every page and responsive breakpoint for text overlap, clipping, misalignment, and unsafe width behavior; identify the exact mobile CSS cause and prepare a fresh deployment-ready checkpoint without directly triggering Vercel deployment. Root cause fixed: raw email text inside a flex contact link lacked a shrinkable width owner beside its trailing icon; wrapped addresses now use min-width: 0, flex: 1 1 auto, and overflow-wrap: anywhere. Full gates and 390px/1440px route previews passed.
- [x] Fix the live mobile composition shown by the user: prevent the fixed chapter dock from covering section content, restore readable single-column Stack/Evidence copy on narrow/coarse-pointer layouts, and verify project/closing/contact flow at mobile and desktop-mode phone widths. Added coarse-pointer rules for wide CSS viewports, forced single-column narrative/project/evidence flow, hid the duplicate fixed chapter dock on touch phones in favor of the header menu, and verified 77 tests, TypeScript, production build, policy gates, formatting, diff hygiene, and 390px/1440px previews.
- [x] Verify and synchronize the latest mobile responsive fix to mantisdarling/Mhini with mantisdarling as both author and committer; do not leave the checkpoint’s Manus metadata on the GitHub branch. GitHub main accepted commit 0eb3e4b authored and committed by mantisdarling; remote head verified via GitHub API.
- [x] Reproduce the user-confirmed live Vercel text misalignment and correct the actual deployed-path layout: keep lower-section copy readable on phone and desktop-mode phone widths, eliminate compressed columns and overlay collisions, and verify against the live composition before handoff. Added the JS-backed `rebuild-touch-layout` marker for narrow touch-capable browsers in desktop mode, forced single-column lower sections, removed the touch chapter-rail overlay, and verified 78 tests, TypeScript, production build, policy gates, and 390px/1440px previews. Vercel still requires publishing to expose the fix live.
- [x] Restore the approved stable source before the later typography/alignment and touch-layout override work, keep the cinematic 404 page and portfolio content, remove the later typography/touch-layout additions, and verify the clean restored baseline. Restored parent `08f79d1b`, reapplied only the 404 implementation from `ef35cfef`, removed the later typography/touch-layout source additions, and verified 76 tests, TypeScript, production build, policy gates, formatting, diff hygiene, plus 390px `/` and `/404` previews.
- [x] Fix the phone-only typography and alignment regression without changing the approved laptop/desktop layout; use the provided mobile screenshot as the target and validate narrow, tablet, and wide breakpoints. Added one isolated max-width: 700px block for mobile width ownership, single-column section flow, safe wrapping, and readable record metadata; desktop selectors were unchanged. Verified 77 tests, TypeScript, production build, all configured policy/security gates, formatting, diff hygiene, and 390px/1440px previews.
- [x] Make the Work section use the desired stacked cinematic project-card layout only on physical touch devices running desktop mode; preserve the existing horizontal swipe carousel in normal phone mode and the approved laptop layout. Added a wide touch-phone detector, applied the single-card cinematic Work stream only under `body.rebuild-desktop-touch-mode`, retained the normal phone `scroll-snap` carousel, and preserved laptop rules. Verified 79 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and the two-mode detector behavior.
- [x] Restore only the earlier working Stack background-image layering behind “Tools are judgment” and add the requested mobile skeleton state for taller cinematic Work images, without reintroducing the removed broad typography or touch-layout changes. Confirmed the deployed image existed but was too subdued at computed opacity 0.3; restored a scoped Stack field class with stronger artwork visibility and layered foreground contrast. Added a visible loading skeleton over each unresolved Work image frame with reduced-motion handling. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and 390px/1440px previews.
- [x] Fix the confirmed live desktop-mode phone regression: make Work render one full-width project card per row, restore clearly visible Stack background artwork behind “Tools are judgment,” and prevent the bottom chapter navigation from covering Stack content without changing laptop typography or normal phone carousel behavior. Added a CSS-level coarse-pointer fallback in addition to the detector, forced stacked Work cards at 701–1100px coarse-pointer widths, hid the obstructing rail, and raised the Stack field visibility. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and screenshot-specific regression assertions.
- [x] Match desktop-mode phone Work cards to the second reference image rather than the current oversized output: use consistent full-width horizontal cards, controlled image height, a readable lower text band, and repeated guide-line rhythm while preserving normal phone carousel and laptop behavior. Rebalanced the scoped wide-touch card art to a 3.2 panoramic ratio, reduced title scale and copy-band height, retained full-width repeated guide lines, and left normal phone/laptop rules untouched. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and reference-specific geometry assertions.
- [x] Remove the visible Work-section grid treatment from the desktop-mode phone composition—card borders, internal guide lines, and grid-gap separators—while preserving project artwork, text bands, normal phone carousel, and laptop layout. Added a scoped coarse-pointer cleanup that removes the Work grid border, separator gap, card outlines, and inline borders only for the desktop-mode phone range; normal phone and laptop styling remain unchanged. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and focused CSS assertions.
- [x] Restore the Work grid system removed by the previous broad cleanup and remove only the unwanted decorative line overlays in the desktop-mode phone composition; preserve card geometry, structural borders, image/text bands, normal phone carousel, and laptop layout. Restored the structural `gap: 1px` and removed only `.rebuild-project-grid::before`/`::after` in the scoped coarse-pointer phone range; no grid system, card geometry, or laptop rules were changed. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and 390px/1440px previews.
- [x] Restore the missing Stack background image behind “Tools are judgment” in the deployed phone composition, using scoped layer/visibility fixes only; do not alter the Work grid, typography, or unrelated sections. Confirmed the deployed image remained in the DOM but the backdrop was suppressed at opacity 0.3; raised only the Stack backdrop to 0.74/0.82, softened its local veil, and added focused assertions. Verified 80 tests, TypeScript, production build, formatting, security/policy gates, diff hygiene, and 390px/1440px previews.
- [x] Audit and fix the reported deployed mobile/desktop-mode state from scratch: make the Stack background support readable “Tools are judgment” copy, align the Stack heading and supporting copy, and move the black chapter navigation out of the middle of the page to an end-of-page-safe position without changing the Work grid or laptop baseline. Added a scoped Stack contrast veil and mobile single-column lockup, forced paper text over the artwork, replaced the fixed touch rail with an in-flow rail after the footer, preserved desktop overlay navigation, and corrected the two stale regressions. Verified 81 tests, TypeScript, production build, formatting, security/policy gates, secret scan, dependency audit, diff hygiene, and responsive previews.
- [x] Perform a repository-wide and deployed-site audit file by file: trace recurring mobile/desktop-mode Stack, Work, navigation, typography, image, and deployment discrepancies to exact root causes; remove conflicting overrides; add targeted regressions; and validate the complete codebase before any new handoff. Root causes documented: duplicate late Stack cascade with contradictory opacity rules, dark base Stack text surviving over artwork, permanently fixed chapter-rail base rules overriding flow placement, and stale indentation-sensitive tests. Consolidated the desktop-touch Work rules, Stack layer, and mobile flow rail; verified 81 tests, TypeScript, production build, unused-export, service-worker, debug-privacy, secret, formatting, diff, dependency-audit gates, plus full 390px and 1440px previews.
- [x] Reproduce the user’s current deployed Vercel screenshot exactly and prove the source/deployment mismatch before making more changes; do not claim the visual fix is complete until the live Work, Stack, navigation, and phone typography paths match the supplied references. Reproduced the old live bundle and computed geometry, then verified the corrected deployment after the hook.
- [x] Verify the Vercel alias serves commit `5d92068` (new HTML ETag/CSS bundle) and re-run the exact phone, desktop-mode phone, laptop, Stack, Work, navigation, and page-end visual checks after the deployment updates. The alias switched to `index-Cq0Mb34y.css` and `index-CkX766Nt.js`; 390px and 1440px renders were reviewed.
- [x] Reproduce the clarified failure where the user refreshes after Vercel reports ready but still cannot see the intended visual changes; inspect the actual delivered HTML/CSS/JS and responsive rendering path rather than attributing the issue only to cache. Confirmed the old CSS/JS bundle and old Work/Stack/rail geometry were still delivered after refresh.
- [x] Trigger one source-neutral GitHub push to test the connected Vercel webhook; use only an empty mantisdarling commit, then verify whether a new Production deployment appears before changing application code. Created and triggered the approved `mantis-production-sync` hook; Vercel produced Ready Production deployments for `875fb70`, and the public alias switched to the current build.
- [x] Perform a careful post-release polish and regression pass across phone, desktop-mode phone, tablet, laptop, and wide desktop; inspect text alignment, media layering, navigation, loading, accessibility, performance, security, runtime errors, and deployment behavior, then fix only verified issues without disturbing the approved composition. Fixed the verified production regression: Stack used `/manus-storage/...`, which returned the deployed 404 route on Vercel; replaced it with the public CDN asset, updated regression coverage, and revalidated 81 tests, TypeScript, build, formatting, and high-severity production dependency audit. The approved composition was otherwise preserved.
- [x] Audit every cinematic background image across narrow phone, desktop-mode phone, tablet, laptop, and wide desktop; verify source variants, crop/focal point, layer ownership, opacity/contrast, loading, section height, duplication, and text overlap, then fix only confirmed placement regressions. Verified 390px, 768px, 1024px, and 1440px full-page renders across hero, Profile, Story, Work, Stack, Evidence, finale, and Contact. The only confirmed issue was the Vercel-invalid Stack `/manus-storage` source, already replaced with the public CDN asset. No additional crop, layer, opacity, duplicate-image, blank-band, or text-overlap regression was justified. Revalidated 81 tests, TypeScript, production build, formatting, and high-severity production dependency audit.
- [x] Build a reversible second-reference preview for the final two chapters: preserve the Evidence image-backed section, place the Closing Motion video in a wide rectangular frame with layered copy, keep Contact separate, and verify phone/tablet/desktop compositions before asking whether to keep or reverse it. Implemented and verified at 390px, 768px, 1440px, and the live 1265px alias; the user approved the push.
- [x] Push the approved second-reference Closing Motion preview: wide rectangular video with layered copy, separate Evidence artwork, and separate Contact section; validate tests/build, sync as mantisdarling, verify Vercel, and confirm the public alias after refresh. Pushed as `mantisdarling` commit `4acf9c9`; 81 tests, TypeScript, build, and formatting passed. Vercel created Ready Production deployment for `4acf9c9`; the public alias serves `index-Dsj5iC5q.css`, and live measurement confirms the 16/9 rectangle with Contact separate below.
- [x] Use the provided katana-on-off-white image as the Contact section background behind “Bring the hard problem closer.”; preserve its rectangular composition, keep the contact card and copy readable above it, avoid image duplication/blending, and verify phone/tablet/desktop before pushing. Corrected the stacking context and replaced the Vercel-unreliable storage path with the public CDN asset; verified 390px/1440px previews and the live alias, where the pseudo-element URL, z-index 0, card z-index 1, section bounds, and no-overflow state are confirmed.
- [x] Push the approved Contact katana background: use the provided katana-on-off-white image behind “Bring the hard problem closer.”, preserve readable foreground copy/card and separate Closing Motion media, verify Vercel and public refresh, then save a checkpoint. Pushed as `mantisdarling` commit `72cbb00`; Vercel served bundle `index-DX9IRDXD.css`, the CDN background URL, Contact z-index layering, and no horizontal overflow after refresh.
- [x] Make the mobile Evidence accordion’s existing artwork expand vertically with the opened content, preserving one continuous background field, smooth height behavior, readable copy, and the approved Contact section; verify closed/open states before pushing. Removed the former fixed 84svh backdrop cap, verified closed and opened states at the mobile breakpoint, confirmed the stabilized opened panel/backdrop geometry, and passed 81 tests, TypeScript, production build, formatting, and dependency audit.
- [x] Push the approved mobile Evidence expansion: the single artwork field follows opened accordion content vertically, with no detached or duplicated image; validate the public refresh and save a checkpoint. Pushed as `mantisdarling` commit `c14c9fe`; Vercel reports the Production deployment Ready, and the refreshed public alias loads the current c14c9fe build with the Evidence controls present.
- [x] Redo the final mobile composition from the supplied phone references: remove the dead black gap after Evidence, keep expanded Evidence artwork continuous, put Closing Motion video and copy inside a correctly sized rectangular frame, and use a safe Contact katana crop/overlay so heading, paragraph, and card do not overlap; audit the entire phone page before asking to push. Removed the final handoff gap, made Closing Motion full-width at 4:5 on phones with copy inside the frame, shifted and softened the Contact katana crop with a text-safe veil, and checked the full 390px page plus the 1440px desktop guard. 81 tests, TypeScript, production build, formatting, and high-severity production audit passed.
- [x] Push the approved phone final-section rebuild from checkpoint `2d8f0bac` as `mantisdarling`, verify Vercel and public refresh, then run a cautious whole-site polish pass across every device, media layer, text block, interaction, loading state, accessibility path, runtime log, build, and security gate; synchronize only verified fixes. Pushed the phone rebuild as `mantisdarling` commit `36e25dc`; added a conservative mobile privacy-banner refinement and a focused final-composition regression, then verified 390px, 768px, and 1440px renders, 82 tests, TypeScript, production build, formatting, dependency audit, and clean runtime/network status. Saved and pushed the polish checkpoint `d555f5f8`; Vercel reports the `d555f5f` Production deployment Ready, and the refreshed public alias serves `index-DkM6af-J.css` and `index-D5J-oJ-Z.js` with no horizontal overflow.
- [x] Reduce the mobile privacy banner’s visual obstruction while preserving consent clarity, accessible controls, and the approved cinematic layout. Scoped the banner to a compact, scroll-safe mobile card with readable copy, clear controls, preserved focus/consent behavior, and no desktop impact.
- [x] Add a smooth mobile Evidence-row slide-open and slide-close animation that preserves accessibility, continuous background expansion, layout stability, and reduced-motion behavior; validate it before pushing. Added a 480ms eased max-height slide with opacity and a 55ms content reveal delay, measured both open and close height progression in the rendered preview, preserved the reduced-motion fallback, and passed 83 tests, TypeScript, production build, formatting, and production dependency audit.
- [x] Refine the mobile Evidence arrow icon to rotate smoothly in sync with row expansion, preserve reduced-motion behavior, push the approved slide animation and refinement as mantisdarling, and verify Vercel/public behavior. Applied a 360ms eased transform rotation on mobile, preserved the reduced-motion rule, and passed 83 tests, TypeScript, production build, formatting, and production dependency audit.
