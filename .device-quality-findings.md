Responsive quality pass findings, 2026-08-22

The final 320x700 capture keeps the hero copy and controls within the viewport, preserves visible cinematic imagery, and prevents the project carousel from creating page-level horizontal overflow. The restored telemetry overlay now appears across project cards, including the touch carousel. The 1440x900 capture preserves the wide editorial composition, original multi-column project grid, full-resolution desktop media, and navigation balance.

Responsive rules now include a safe overflow fallback for older engines, explicit horizontal touch intent, smooth snap scrolling, focus styling for keyboard users, a safer small-phone hero minimum, and an image overlay that restores the project grid treatment after the mobile carousel changes. Reduced-motion behavior remains covered by existing global rules.
